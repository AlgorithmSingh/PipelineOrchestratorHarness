# Pipeline Orchestrator Harness

Deterministic TypeScript orchestrator for Plan Generation, Execution, and Adversarial pipelines.

## Quick Start

```bash
npm install
npm run typecheck
npm test
npm run dev -- config validate
npm run dev -- status
```

Run one execution polling cycle:

```bash
npm run dev -- start --pipeline execution --once
```
